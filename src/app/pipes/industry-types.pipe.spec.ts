import { IndustryTypesPipe } from './industry-types.pipe';

describe('IndustryTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new IndustryTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
